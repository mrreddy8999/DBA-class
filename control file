//To see control files
after connecting to database

show parameter control_files;
alter system set <parameter_name> = <value> scope = both/spfile/memory;
alter SYSTEM set control_files = '/u01/app/oracle/oradata/PRODDB/controlfile/o1_mf_krlzjxt8_.ctl', '/u01/app/oracle/fast_recovery_area/PRODDB/controlfile/o1_mf_krlzjxtz_.ctl', '/u01/multiplex/control_file/control003.ctl' scope = spfile;
shut immediate;
copy present control file to new control file in bin
cp /u01/app/oracle/oradata/PRODDB/controlfile/control003.ctl /u01/multiplex/control_file/
check the control file using commad ls- lrt in the path :
/u01/multiplex/control_file
  moving the control file of presnt to new .
then mv control003.ctl control003.ctl 
  ls-lrt
  we can see the cntrl file with new name.
  startup;
